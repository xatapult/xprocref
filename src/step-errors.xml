<?xml version="1.0" encoding="UTF-8"?>
<errors xsi:schemaLocation="http://www.xtpxlib.nl/ns/xprocref ../xsd/xprocref.xsd" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://www.xtpxlib.nl/ns/xprocref">

  <!-- ======================================================================= -->
  <!-- == Errors for: Standard steps == -->

  <error code="XC0001">
    <!-- == p:text-join == -->
    <description>
      <para>It is a dynamic error if the value of option <option>override-content-type</option> is not a text media type.</para>
    </description>
  </error>
  <error code="XC0003">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if a “<literal>username</literal>” or a “<literal>password</literal>” key is present without specifying a value for
        the “<literal>auth-method</literal>” key, if the requested <literal>auth-method</literal> isn't supported, or the authentication challenge
        contains an authentication method that isn't supported.</para>
    </description>
  </error>
  <error code="XC0007">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if any key in <option>parameters</option> is associated to a value which is not an instance of the XQuery 1.0 and
        XPath 2.0 Data Model, e.g. with a map, an array, or a function.</para>
    </description>
  </error>
  <error code="XC0008">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if the stylesheet does not support a given mode.</para>
    </description>
  </error>
  <error code="XC0009">
    <!-- == p:xquery == -->
    <description>
      <para>It is a dynamic error if the specified XQuery version is not available.</para>
    </description>
  </error>
  <error code="XC0013">
    <!-- == p:rename == -->
    <description>
      <para>It is a dynamic error if the pattern matches a processing instruction and the new name has a non-null namespace.</para>
    </description>
  </error>
  <error code="XC0014">
    <!-- == p:namespace-rename == -->
    <description>
      <para>It is a dynamic error if the XML namespace (<uri>http://www.w3.org/XML/1998/namespace</uri>) or the XMLNS namespace
          (<uri>http://www.w3.org/2000/xmlns/</uri>) is the value of either the <option>from</option> option or the <option>to</option> option.</para>
    </description>
  </error>
  <error code="XC0019">
    <!-- == p:compare == -->
    <description>
      <para>It is a dynamic error if the documents are not equal according to the specified comparison <option>method</option>, and the value of the
          <option>fail-if-not-equal</option> option is <literal>true</literal>.</para>
    </description>
  </error>
  <error code="XC0023">
    <!-- == p:add-attribute == -->
    <description>
      <para>It is a dynamic error if the selection pattern matches a wrong type of node.</para>
    </description>
    <!-- == p:add-attribute - It is a dynamic error if the selection pattern matches a node which is not an element. -->
    <!-- == p:delete - It is a dynamic error if the match option matches the document node. == -->
    <!-- == p:insert - It is a dynamic error if that pattern matches an attribute or a namespace node. == -->
    <!-- == p:label-elements - It is a dynamic error if that expression matches anything other than element nodes. == -->
    <!-- == p:make-absolute-uris - It is a dynamic error if the pattern matches anything other than element or attribute nodes. == -->
    <!-- == p:rename - It is a dynamic error if the pattern matches anything other than element, attribute or processing instruction nodes. == -->
    <!-- == p:replace - It is a dynamic error if that pattern matches an attribute or a namespace nodes. == -->
    <!-- == p:set-attributes - It is a dynamic error if that pattern matches anything other than element nodes. == -->
    <!-- == p:unwrap - It is a dynamic error if that pattern matches anything other than the document node or element nodes. == -->
    <!-- == p:wrap - It is a dynamic error if the pattern matches anything other than document, element, text, processing instruction, and comment nodes. == -->
  </error>
  <error code="XC0024">
    <!-- == p:insert == -->
    <description>
      <para>It is a dynamic error if the selection pattern matches a document node and the value of the <option>position</option> is
          “<literal>before</literal>” or “<literal>after</literal>”.</para>
    </description>
  </error>
  <error code="XC0025">
    <!-- == p:insert == -->
    <description>
      <para>It is a dynamic error if the selection pattern matches anything other than an element or a document node and the value of the
          <option>position</option> option is “<literal>first-child</literal>” or “<literal>last-child</literal>”.</para>
    </description>
  </error>
  <error code="XC0029">
    <!-- == p:xinclude == -->
    <description>
      <para>It is a dynamic error if an XInclude error occurs during processing.</para>
    </description>
  </error>
  <error code="XC0030">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the response body cannot be interpreted as requested (e.g. <literal>application/json</literal> to override
          <literal>application/xml</literal> content).</para>
    </description>
  </error>
  <error code="XC0036">
    <!-- == p:hash == -->
    <description>
      <para>It is a dynamic error if the requested hash algorithm is not one that the processor understands or if the value or parameters are not
        appropriate for that algorithm.</para>
    </description>
  </error>
  <error code="XC0037">
    <!-- == p:www-form-urldecode == -->
    <description>
      <para>It is a dynamic error if the <option>value</option> provided is not a properly <literal>x-www-form-urlencoded</literal> value.</para>
    </description>
  </error>
  <error code="XC0038">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if the specified xslt version is not available.</para>
    </description>
  </error>
  <error code="XC0039">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if the source port does not contain exactly one XML document or one HTML document if XSLT 1.0 is used.</para>
    </description>
  </error>
  <error code="XC0056">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if the stylesheet does not provide a given template.</para>
    </description>
  </error>
  <error code="XC0058">
    <!-- == p:add-xml-base == -->
    <description>
      <para>It is a dynamic error if the <option>all</option> and <option>relative</option> options are both <code>true</code>.</para>
    </description>
  </error>
  <error code="XC0059">
    <!-- == p:add-attribute == -->
    <description>
      <para>It is a dynamic error if the QName value in the <option>attribute-name</option> option uses the prefix “<literal>xmlns</literal>” or any
        other prefix that resolves to the namespace name <uri>http://www.w3.org/2000/xmlns/</uri>. </para>
    </description>
  </error>
  <error code="XC0060">
    <!-- == p:uuid == -->
    <description>
      <para>It is a dynamic error if the processor does not support the specified <option>version</option> of the UUID algorithm.</para>
    </description>
  </error>
  <error code="XC0062">
    <!-- == p:delete == -->
    <description>
      <para>It is a dynamic error if the <option>match</option> option matches a namespace node.</para>
    </description>
  </error>
  <error code="XC0069">
    <!-- == p:set-properties == -->
    <description>
      <para>It is a dynamic error if the <option>properties</option> map contains a key equal to the string “<literal>content-type</literal>”.</para>
    </description>
  </error>
  <error code="XC0071">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the <tag>p:cast-content-type</tag> step cannot perform the requested cast.</para>
    </description>
  </error>
  <error code="XC0072">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the <tag>c:data</tag> contains content is not a valid base64 string.</para>
    </description>
  </error>
  <error code="XC0073">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the <tag>c:data</tag> element does not have a <tag class="attribute">content-type</tag> attribute.</para>
    </description>
  </error>
  <error code="XC0074">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the <option>content-type</option> is supplied and is not the same as the <tag class="attribute"
          >content-type</tag> specified on the <tag>c:data</tag> element.</para>
    </description>
  </error>
  <error code="XC0076">
    <!-- == p:compare == -->
    <description>
      <para>It is a dynamic error if the comparison <option>method</option> specified in <tag>p:compare</tag> is not supported by the
        implementation.</para>
    </description>
  </error>
  <error code="XC0077">
    <!-- == p:compare == -->
    <description>
      <para>It is a dynamic error if the media types of the documents supplied are incompatible with the comparison <option>method</option>.</para>
    </description>
  </error>
  <error code="XC0078">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the value associated with the “<literal>fail-on-timeout</literal>” is associated with <code>true()</code> and a
        HTTP status code <literal>408</literal> is encountered.</para>
    </description>
  </error>
  <error code="XC0079">
    <!-- == p:archive-manifest == -->
    <description>
      <para>It is a dynamic error if the map <option>parameters</option> contains an entry whose key is defined by the implementation and whose value
        is not valid for that key.</para>
    </description>
    <!-- == p:archive - It is a dynamic error if the map parameters contains an entry whose key is defined by the implementation and whose value is not valid for that key. == -->
    <!-- == p:cast-content-type - It is a dynamic error if the map parameters contains an entry whose key is defined by the implementation and whose value is not valid for that key. == -->
    <!-- == p:compress - It is a dynamic error if the map parameters contains an entry whose key is defined by the implementation and whose value is not valid for that key. == -->
    <!-- == p:unarchive - It is a dynamic error if the map parameters contains an entry whose key is defined by the implementation and whose value is not valid for that key. == -->
    <!-- == p:uncompress - It is a dynamic error if the map parameters contains an entry whose key is defined by the implementation and whose value is not valid for that key. == -->
  </error>
  <error code="XC0080">
    <!-- == p:archive == -->
    <description>
      <para>It is a dynamic error if the number of documents on the <port>archive</port> does not match the expected number of archive input documents
        for the given <code>format</code> and <code>command</code>.</para>
    </description>
  </error>
  <error code="XC0081">
    <!-- == p:archive == -->
    <description>
      <para>It is a dynamic error if the format of the archive does not match the format as specified in the <option>format</option> option.</para>
    </description>
  </error>
  <error code="XC0083">
    <!-- == p:parameters == -->
    <description>
      <para>It is a dynamic error if the <tag class="attribute">namespace</tag> attribute is specified, the <tag class="attribute">name</tag> contains
        a colon, and the specified namespace is not the same as the in-scope namespace binding for the specified prefix.</para>
    </description>
  </error>
  <error code="XC0084">
    <!-- == p:archive == -->
    <description>
      <para>It is a dynamic error if two or more documents appear on the <code>p:archive</code> step's <port>source</port> port that have the same
        base URI or if any document that appears on the <port>source</port> port has no base URI.</para>
    </description>
    <!-- == p:parameters - It is a dynamic error if the parameter list contains any elements other than c:param. == -->
  </error>
  <error code="XC0085">
    <!-- == p:archive-manifest == -->
    <description>
      <para>It is a dynamic error if the format of the archive does not match the specified format, cannot be understood, determined and/or
        processed.</para>
    </description>
    <!-- == p:archive - It is a dynamic error if the format of the archive does not match the specified format, cannot be understood, determined and/or processed. == -->
    <!-- == p:parameters - It is a dynamic error for any unqualified attribute names other than “name”, “namespace”, “as”, or “value” to appear on a c:param element. == -->
    <!-- == p:unarchive - It is a dynamic error if the format of the archive does not match the specified format, cannot be understood, determined and/or processed. == -->
  </error>
  <error code="XC0086">
    <!-- == p:parameters == -->
    <description>
      <para>It is a dynamic error for any unqualified attribute names to appear on a <tag>c:param-set</tag> element.</para>
    </description>
  </error>
  <error code="XC0087">
    <!-- == p:parameters == -->
    <description>
      <para>It is a dynamic error if the <tag class="attribute">namespace</tag> attribute is not specified, the <tag class="attribute">name</tag>
        contains a colon, and the specified prefix is not in the in-scope namespace bindings.</para>
    </description>
  </error>
  <error code="XC0089">
    <!-- == p:parameters == -->
    <description>
      <para>It is a dynamic error if the sequence type is not syntactically valid.</para>
    </description>
  </error>
  <error code="XC0092">
    <!-- == p:namespace-rename == -->
    <description>
      <para>It is a dynamic error if as a consequence of changing or removing the namespace of an attribute the attribute's name is not unique on the
        respective element.</para>
    </description>
  </error>
  <error code="XC0093">
    <!-- == p:xslt == -->
    <description>
      <para> It is a dynamic error if a static error occurs during the static analysis of the XSLT stylesheet.</para>
    </description>
  </error>
  <error code="XC0094">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if any document supplied on the source port is not an XML document, an HTML documents, or a Text document if XSLT
        2.0 is used.</para>
    </description>
  </error>
  <error code="XC0095">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if an error occurred during the transformation.</para>
    </description>
  </error>
  <error code="XC0096">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if the transformation is terminated by XSLT message termination.</para>
    </description>
  </error>
  <error code="XC0098">
    <!-- == p:text-sort == -->
    <description>
      <para>It is a dynamic error if a dynamic XPath error occurred while applying sort-key to a line.</para>
    </description>
  </error>
  <error code="XC0099">
    <!-- == p:text-sort == -->
    <description>
      <para>It is a dynamic error if the result of applying <code>sort-key</code> to a given line results in a sequence with more than one
        item.</para>
    </description>
  </error>
  <error code="XC0100">
    <!-- == p:archive == -->
    <description>
      <para>It is a dynamic error if the document on port <port>manifest</port> does not conform to the given schema.</para>
    </description>
  </error>
  <error code="XC0101">
    <!-- == p:xquery == -->
    <description>
      <para>It is a dynamic error if a document appearing on port <port>source</port> cannot be represented in the XDM version associated with the
        chosen XQuery version, e.g. when a JSON document contains a map and XDM 3.0 is used.</para>
    </description>
  </error>
  <error code="XC0102">
    <!-- == p:xquery == -->
    <description>
      <para>It is a dynamic error if any key in option <option>parameters</option> is associated to a value that cannot be represented in the XDM
        version associated with the chosen XQuery version, e.g. with a map, an array, or a function when XDM 3.0 is used.</para>
    </description>
  </error>
  <error code="XC0103">
    <!-- == p:xquery == -->
    <description>
      <para>It is a dynamic error if any error occurs during XQuery’s static analysis phase.</para>
    </description>
  </error>
  <error code="XC0104">
    <!-- == p:xquery == -->
    <description>
      <para>It is a dynamic error if any error occurs during XQuery’s dynamic evaluation phase.</para>
    </description>
  </error>
  <error code="XC0105">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if an XSLT 1.0 stylesheet is invoked and option <option>parameters</option> contains a value that is not an atomic
        value or a node.</para>
    </description>
  </error>
  <error code="XC0106">
    <!-- == p:json-merge == -->
    <description>
      <para>It is a dynamic error if duplicate keys are encountered and option <option>duplicates</option> has value
        “<literal>reject</literal>”.</para>
    </description>
  </error>
  <error code="XC0107">
    <!-- == p:json-merge == -->
    <description>
      <para> It is a dynamic error if a document of a not supported document type appears on port <port>source</port> of
        <code>p:json-merge</code>.</para>
    </description>
  </error>
  <error code="XC0108">
    <!-- == p:namespace-delete == -->
    <description>
      <para>It is a dynamic error if any prefix is not in-scope at the point where the <step/> occurs.</para>
    </description>
  </error>
  <error code="XC0109">
    <!-- == p:namespace-delete == -->
    <description>
      <para>It is a dynamic error if a namespace is to be removed from an attribute and the element already has an attribute with the resulting name.
        For instance, removing the namespace with the <code>ns1</code> prefix will raise this error when applied to <tag>something ns1:status="ok"
          status="bad"/</tag>.</para>
    </description>
  </error>
  <error code="XC0110">
    <!-- == p:json-merge == -->
    <description>
      <para>It is a dynamic error if the evaluation of the XPath expression in option <option>key</option> for a given item returns either a sequence,
        an array, a map, or a function.</para>
    </description>
  </error>
  <error code="XC0111">
    <!-- == p:json-join == -->
    <description>
      <para> It is a dynamic error if a document of an unsupported document type appears on port <port>source</port> of
        <code>p:json-join</code>.</para>
    </description>
  </error>
  <error code="XC0112">
    <!-- == p:archive == -->
    <description>
      <para>It is a dynamic error if more than one document appears on the port <port>manifest</port>.</para>
    </description>
  </error>
  <error code="XC0117">
    <description>
      <para>It is a dynamic error if a report-format option was specified that the processor does not support.</para>
    </description>
  </error>
  <error code="XC0118">
    <!-- == p:archive == -->
    <description>
      <para>It is a dynamic error if an archive manifest is invalid according to the specification.</para>
    </description>
  </error>
  <error code="XC0119">
    <!-- == p:json-join == -->
    <description>
      <para>It is a dynamic error if <option>flatten</option> is neither “<literal>unbounded</literal>”, nor a string that may be cast to a
        non-negative integer.</para>
    </description>
  </error>
  <error code="XC0120">
    <!-- == p:archive-manifest == -->
    <description>
      <para>It is a dynamic error if the <option>relative-to</option> option is not present and the document on the <port>source</port> port does not
        have a base URI.</para>
    </description>
    <!-- == p:unarchive - It is a dynamic error if the relative-to option is not present and the document on the source port does not have a base URI. == -->
  </error>
  <error code="XC0121">
    <!-- == p:xslt == -->
    <description>
      <para>It is a dynamic error if a document appearing on the <port>secondary</port> port has a base URI that is not both absolute and valid
        according to <link xlink:href="https://www.rfc-editor.org/info/rfc3986" role="newpage" xmlns:xlink="http://www.w3.org/1999/xlink"
          >RFC 3986</link> .</para>
    </description>
  </error>
  <error code="XC0122">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the given method is not supported.</para>
    </description>
  </error>
  <error code="XC0123">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if any key in the “<literal>auth</literal>” map is associated with a value that is not an instance of the required
        type.</para>
    </description>
  </error>
  <error code="XC0124">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if any key in the “parameters” map is associated with a value that is not an instance of the required type.</para>
    </description>
  </error>
  <error code="XC0125">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the key “<literal>accept-multipart</literal>” as the value <code>false()</code> and a multipart response is
        detected.</para>
    </description>
  </error>
  <error code="XC0126">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the XPath expression in <option>assert</option> evaluates to <code>false</code>.</para>
    </description>
  </error>
  <error code="XC0127">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the <option>headers</option> map contains two keys that are the same when compared in a case-insensitive
        manner.</para>
    </description>
  </error>
  <error code="XC0128">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the URI’s scheme is unknown or not supported.</para>
    </description>
  </error>
  <error code="XC0129">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the requested HTTP version is not supported.</para>
    </description>
  </error>
  <error code="XC0131">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the processor cannot support the requested encoding.</para>
    </description>
  </error>
  <error code="XC0132">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the override content encoding cannot be supported.</para>
    </description>
  </error>
  <error code="XC0133">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if more than one document appears on the <port>source</port> port and a <literal>content-type</literal> header is
        present and the content type specified is not a multipart content type.</para>
    </description>
  </error>
  <error code="XC0146">
    <!-- == p:archive-manifest == -->
    <description>
      <para>It is a dynamic error if the specified value for the <option>override-content-types</option> option is not an array of arrays, where the
        inner arrays have exactly two members of type <code>xs:string</code>.</para>
    </description>
  </error>
  <error code="XC0147">
    <!-- == p:archive-manifest == -->
    <description>
      <para>It is a dynamic error if the specified value is not a valid XPath regular expression.</para>
    </description>
    <!-- == p:text-replace - It is a dynamic error if the specified value is not a valid XPath regular expression. == -->
    <!-- == p:unarchive - It is a dynamic error if a specified value is not a valid XPath regular expression. == -->
  </error>
  <error code="XC0150">
    <!-- == p:split-sequence == -->
    <description>
      <para>It is a dynamic error if evaluating the XPath expression in option <option>test</option> on a context document results in an error.</para>
    </description>
  </error>
  <error code="XC0201">
    <!-- == p:uncompress == -->
    <description>
      <para>It is a dynamic error if the <tag>p:uncompress</tag> step cannot perform the requested content-type cast.</para>
    </description>
  </error>
  <error code="XC0202">
    <!-- == p:compress == -->
    <description>
      <para>It is a dynamic error if the compression format cannot be understood, determined and/or processed.</para>
    </description>
    <!-- == p:uncompress - It is a dynamic error if the compression format cannot be understood, determined and/or processed. == -->
  </error>
  <error code="XC0203">
    <!-- == p:http-request == -->
    <description>
      <para>It is a dynamic error if the specified boundary is not valid (for example, if it begins with two hyphens “--”).</para>
    </description>
  </error>
  <error code="XD0011">
    <!-- == p:archive == -->
    <description>
      <para>It is a dynamic error if the resource referenced by the <option>href</option> option does not exist, cannot be accessed or is not a
        file.</para>
    </description>
    <!-- == p:load - It is a dynamic error if the resource referenced by a p:load element does not exist or cannot be accessed. == -->
  </error>
  <error code="XD0023">
    <!-- == p:load == -->
    <description>
      <para>It is a dynamic error if a DTD validation is performed and either the document is not valid or no DTD is found.</para>
    </description>
  </error>
  <error code="XD0036">
    <!-- == p:parameters == -->
    <description>
      <para>It is a dynamic error if the supplied value of a variable or option cannot be converted to the required type.</para>
    </description>
  </error>
  <error code="XD0043">
    <!-- == p:load == -->
    <description>
      <para>It is a dynamic error if the <option>dtd-validate</option> parameter is <literal>true</literal> and the processor does not support DTD
        validation.</para>
    </description>
  </error>
  <error code="XD0049">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the text value is not a well-formed XML document</para>
    </description>
    <!-- == p:load - It is a dynamic error if the loaded content is not a well-formed XML document. == -->
  </error>
  <error code="XD0057">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the text document does not conform to the JSON grammar, unless the parameter liberal is true and the processor
        chooses to accept the deviation.</para>
    </description>
    <!-- == p:load - It is a dynamic error if the loaded content does not conform to the JSON grammar, unless the parameter liberal is true and the processor chooses to accept the deviation. == -->
  </error>
  <error code="XD0058">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the parameter duplicates is reject and the text document contains a JSON object with duplicate keys.</para>
    </description>
    <!-- == p:load - It is a dynamic error if the parameter duplicates is reject and the value of loaded content contains a JSON object with duplicate keys. == -->
  </error>
  <error code="XD0059">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the parameter map contains an entry whose key is defined in the specification of
          <function>fn:parse-json</function> and whose value is not valid for that key, or if it contains an entry with the key fallback when the
        parameter <literal>escape</literal> with <literal>true()</literal> is also present.</para>
    </description>
    <!-- == p:load - It is a dynamic error if the parameter map contains an entry whose key is defined in the specification of fn:parse-json and whose value is not valid for that key, or if it contains an entry with the key fallback when the parameter escape with true() is also present. == -->
  </error>
  <error code="XD0060">
    <!-- == p:cast-content-type == -->
    <description>
      <para>It is a dynamic error if the text document can not be converted into the XPath data model</para>
    </description>
    <!-- == p:load - It is a dynamic error if the content-type specifies an encoding, which is not supported by the processor. == -->
  </error>
  <error code="XD0062">
    <!-- == p:load == -->
    <description>
      <para>It is a dynamic error if the <tag class="attribute">content-type</tag> is specified and the document-properties has a
          “<code>content-type</code>” that is not the same.</para>
    </description>
  </error>
  <error code="XD0064">
    <!-- == p:archive-manifest == -->
    <description>
      <para>It is a dynamic error if the base URI is not both absolute and valid according to <link
          xlink:href="https://www.rfc-editor.org/info/rfc3986" role="newpage" xmlns:xlink="http://www.w3.org/1999/xlink">RFC 3986</link> .</para>
    </description>
    <!-- == p:archive - It is a dynamic error if the base URI is not both absolute and valid according to RFC 3986 . == -->
    <!-- == p:load - It is a dynamic error if the base URI is not both absolute and valid according to RFC 3986 . == -->
    <!-- == p:make-absolute-uris - It is a dynamic error if the base URI is not both absolute and valid according to RFC 3986 . == -->
    <!-- == p:set-properties - It is a dynamic error if the base URI is not both absolute and valid according to RFC 3986 . == -->
    <!-- == p:unarchive - It is a dynamic error if the option is not a valid URI according to RFC 3986 . == -->
  </error>
  <error code="XD0070">
    <!-- == p:set-properties == -->
    <description>
      <para>It is a dynamic error if a value is assigned to the <code>serialization</code> document property that cannot be converted into
          <code>map(xs:QName, item()*)</code> according to the rules in section “QName handling” of <link xlink:href="https://xproc.org/"
          role="newpage" xmlns:xlink="http://www.w3.org/1999/xlink">XProc 3.0</link> .</para>
    </description>
  </error>
  <error code="XD0078">
    <!-- == p:load == -->
    <description>
      <para>It is a dynamic error if the loaded document cannot be represented as an HTML document in the XPath data model.</para>
    </description>
  </error>
  <error code="XD0079">
    <!-- == p:archive-manifest == -->
    <description>
      <para>It is a dynamic error if a supplied content-type is not a valid media type of the form “<literal>
          <replaceable>type</replaceable>/<replaceable>subtype</replaceable>+<replaceable>ext</replaceable>
        </literal>” or “<literal>
          <replaceable>type</replaceable>/<replaceable>subtype</replaceable>
        </literal>”.</para>
    </description>
    <!-- == p:cast-content-type - It is a dynamic error if a supplied content-type is not a valid media type of the form “type/subtype+ext” or “type/subtype”. == -->
    <!-- == p:http-request - It is a dynamic error if a supplied content-type is not a valid media type of the form “type/subtype+ext” or “type/subtype”. == -->
    <!-- == p:load - It is a dynamic error if a supplied content-type is not a valid media type of the form “type/subtype+ext” or “type/subtype”. == -->
    <!-- == p:text-join - It is a dynamic error if a supplied content-type is not a valid media type of the form “type/subtype+ext” or “type/subtype”. == -->
    <!-- == p:uncompress - It is a dynamic error if a supplied content-type is not a valid media type of the form “type/subtype+ext” or “type/subtype”. == -->
  </error>

  <!-- ======================================================================= -->
  <!-- == Errors for: file steps == -->

  <error code="XC0012">
    <!-- == p:directory-list == -->
    <description>
      <para>It is a dynamic error if the contents of the directory path are not available to the step due to access restrictions in the environment in
        which the pipeline is run.</para>
    </description>
  </error>
  <error code="XC0017">
    <!-- == p:directory-list == -->
    <description>
      <para>It is a dynamic error if the absolute path does not identify a directory.</para>
    </description>
  </error>
  <error code="XC0050">
    <!-- == p:file-copy == -->
    <description>
      <para>It is a dynamic error the file or directory cannot be copied to the specified location.</para>
    </description>
    <!-- == p:file-move - It is a dynamic error if the directory cannot be moved to the specified location. == -->
  </error>
  <error code="XC0090">
    <!-- == p:directory-list == -->
    <description>
      <para>It is a dynamic error if an implementation does not support directory listing for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0113">
    <!-- == p:file-delete == -->
    <description>
      <para>It is a dynamic error if an attempt is made to delete a non-empty directory and the <option>recursive</option> option was set to
          <code>false</code>.</para>
    </description>
  </error>
  <error code="XC0114">
    <!-- == p:file-mkdir == -->
    <description>
      <para>It is a dynamic error if the directory referenced by the <option>href</option> option cannot be created.</para>
    </description>
  </error>
  <error code="XC0115">
    <!-- == p:file-move == -->
    <description>
      <para>It is a dynamic error if the resource referenced by the <option>target</option> option is an existing file or other file system
        object.</para>
    </description>
  </error>
  <error code="XC0116">
    <!-- == p:file-create-tempfile == -->
    <description>
      <para>It is a dynamic error if the temporary file could not be created.</para>
    </description>
  </error>
  <error code="XC0134">
    <!-- == p:file-info == -->
    <description>
      <para>It is a dynamic error if an implementation does not support <tag>p:file-info</tag> for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0135">
    <!-- == p:file-info == -->
    <description>
      <para>It is a dynamic error if <tag>p:file-info</tag> is not available to the step due to access restrictions in the environment in which the
        pipeline is run.</para>
    </description>
  </error>
  <error code="XC0136">
    <!-- == p:file-touch == -->
    <description>
      <para>It is a dynamic error if an implementation does not support <tag>p:file-touch</tag> for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0137">
    <!-- == p:file-touch == -->
    <description>
      <para>It is a dynamic error if <tag>p:file-touch</tag> cannot be completed due to access restrictions in the environment in which the pipeline
        is run.</para>
    </description>
  </error>
  <error code="XC0138">
    <!-- == p:file-create-tempfile == -->
    <description>
      <para>It is a dynamic error if an implementation does not support <tag>p:file-create-tempfile</tag> for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0139">
    <!-- == p:file-create-tempfile == -->
    <description>
      <para>It is a dynamic error if <tag>p:file-create-tempfile</tag> cannot be completed due to access restrictions in the environment in which the
        pipeline is run.</para>
    </description>
  </error>
  <error code="XC0140">
    <!-- == p:file-mkdir == -->
    <description>
      <para>It is a dynamic error if an implementation does not support <tag>p:file-mkdir</tag> for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0141">
    <!-- == p:file-mkdir == -->
    <description>
      <para>It is a dynamic error if <tag>p:file-mkdir</tag> not available to the step due to access restrictions in the environment in which the
        pipeline is run.</para>
    </description>
  </error>
  <error code="XC0142">
    <!-- == p:file-delete == -->
    <description>
      <para>It is a dynamic error if an implementation does not support <tag>p:file-delete</tag> for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0143">
    <!-- == p:file-delete == -->
    <description>
      <para>It is a dynamic error if <tag>p:file-delete</tag> is not available to the step due to access restrictions in the environment in which the
        pipeline is run.</para>
    </description>
  </error>
  <error code="XC0144">
    <!-- == p:file-copy == -->
    <description>
      <para>It is a dynamic error if an implementation does not support <tag>p:file-copy</tag> for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0145">
    <!-- == p:file-copy == -->
    <description>
      <para>It is a dynamic error if <tag>p:file-copy</tag> is not available to the step due to access restrictions in the environment in which the
        pipeline is run.</para>
    </description>
  </error>
  <error code="XC0148">
    <!-- == p:file-move == -->
    <description>
      <para>It is a dynamic error if an implementation does not support <tag>p:file-move</tag> for a specified scheme.</para>
    </description>
  </error>
  <error code="XC0149">
    <!-- == p:file-move == -->
    <description>
      <para>It is a dynamic error if <tag>p:file-move</tag> is not available to the step due to access restrictions in the environment in which the
        pipeline is run.</para>
    </description>
  </error>
  <error code="XC0157">
    <!-- == p:file-copy == -->
    <description>
      <para>It is a dynamic error if the <option>href</option> option names a directory, but the <option>target</option> option names a file.</para>
    </description>
  </error>
  <error code="XC0158">
    <!-- == p:file-move == -->
    <description>
      <para>It is a dynamic error if the <option>href</option> option names a directory, but the <option>target</option> option names a file.</para>
    </description>
  </error>

  <!-- ======================================================================= -->
  <!-- == Errors for: os steps == -->

  <error code="XC0032">
    <!-- == p:os-exec == -->
    <description>
      <para>It is a dynamic error if more than one document appears on the <port>source</port> port of the <tag>p:os-exec</tag> step.</para>
    </description>
  </error>
  <error code="XC0033">
    <!-- == p:os-exec == -->
    <description>
      <para>It is a dynamic error if the command cannot be run.</para>
    </description>
  </error>
  <error code="XC0034">
    <!-- == p:os-exec == -->
    <description>
      <para>It is a dynamic error if the current working directory cannot be changed to the value of the <option>cwd</option> option.</para>
    </description>
  </error>
  <error code="XC0063">
    <!-- == p:os-exec == -->
    <description>
      <para>It is a dynamic error if the <option>path-separator</option> option is specified and is not exactly one character long.</para>
    </description>
  </error>
  <error code="XC0064">
    <!-- == p:os-exec == -->
    <description>
      <para>It is a dynamic error if the exit code from the command is greater than the specified <option>failure-threshold</option> value.</para>
    </description>
  </error>

  <!-- ======================================================================= -->
  <!-- == Errors for: mail steps == -->

  <error code="XC0159">
    <!-- == p:send-mail == -->
    <description>
      <para>It is a dynamic error if any key in the “<literal>auth</literal>” map is associated with a value that is not an instance of the required
        type.</para>
    </description>
  </error>
  <error code="XC0160">
    <!-- == p:send-mail == -->
    <description>
      <para>It is a dynamic error if any key in the “<literal>parameters</literal>” map is associated with a value that is not an instance of the
        required type.</para>
    </description>
  </error>
  <error code="XC0161">
    <!-- == p:send-mail == -->
    <description>
      <para>It is a dynamic error if the first document on the <port>source</port> port does not conform to the required format.</para>
    </description>
  </error>
  <error code="XC0162">
    <!-- == p:send-mail == -->
    <description>
      <para>It is a dynamic error if the email cannot be send.</para>
    </description>
  </error>

  <!-- ======================================================================= -->
  <!-- == Errors for: paged-media steps == -->

  <error code="XC0166">
    <!-- == p:css-formatter == -->
    <description>
      <para>It is a dynamic error if the requested document cannot be produced.</para>
    </description>
  </error>
  <error code="XC0167">
    <!-- == p:xsl-formatter == -->
    <description>
      <para>It is a dynamic error if the requested document cannot be produced.</para>
    </description>
  </error>
  <error code="XC0204">
    <!-- == p:css-formatter == -->
    <description>
      <para>It is a dynamic error if the requested content-type is not supported.</para>
    </description>
    <!-- == p:xsl-formatter - It is a dynamic error if the requested content-type is not supported. == -->
  </error>

  <!-- ======================================================================= -->
  <!-- == Errors for: validation steps == -->
  <error code="XC0011">
    <!-- == p:validate-with-xml-schema == -->
    <description>
      <para>It is a dynamic error if the specified schema version is not available.</para>
    </description>
  </error>
  <error code="XC0053">
    <!-- == p:validate-with-nvdl == -->
    <description>
      <para>It is a dynamic error if the <option>assert-valid</option> option on <tag>p:validate-with-nvdl</tag> is <literal>true</literal> and the
        input document is not valid.</para>
    </description>
  </error>
  <error code="XC0054">
    <!-- == p:validate-with-schematron == -->
    <description>
      <para>It is a dynamic error if the <option>assert-valid</option> option is <literal>true</literal> and any Schematron assertions fail.</para>
    </description>
  </error>
  <error code="XC0055">
    <!-- == p:validate-with-xml-schema == -->
    <description>
      <para>It is a dynamic error if the implementation does not support the specified mode.</para>
    </description>
  </error>
  <error code="XC0151">
    <!-- == p:validate-with-schematron == -->
    <description>
      <para>It is a dynamic error if the document supplied on <port>schema</port> port is not a valid Schematron document.</para>
    </description>
  </error>
  <error code="XC0152">
    <!-- == p:validate-with-xml-schema == -->
    <description>
      <para>It is a dynamic error if the document supplied on <port>schema</port> port is not a valid XML schema document.</para>
    </description>
  </error>
  <error code="XC0153">
    <!-- == p:validate-with-relax-ng == -->
    <description>
      <para>It is a dynamic error if the document supplied on <port>schema</port> port cannot be interpreted as an RELAX NG Grammar. </para>
    </description>
  </error>
  <error code="XC0154">
    <!-- == p:validate-with-nvdl == -->
    <description>
      <para>It is a dynamic error if the document supplied on <port>nvdl</port> port is not a valid NVDL document.</para>
    </description>
  </error>
  <error code="XC0155">
    <!-- == p:validate-with-relax-ng == -->
    <description>
      <para>It is a dynamic error if the <option>assert-valid</option> option on <tag>p:validate-with-relax-ng</tag> is <literal>true</literal> and
        the input document is not valid.</para>
    </description>
  </error>
  <error code="XC0156">
    <!-- == p:validate-with-xml-schema == -->
    <description>
      <para>It is a dynamic error if the <option>assert-valid</option> option on <tag>p:validate-with-xml-schema</tag> is <literal>true</literal> and
        the input document is not valid.</para>
    </description>
  </error>
  <error code="XC0163">
    <!-- == p:validate-with-json-schema == -->
    <description>
      <para>It is a dynamic error if the selected version is not supported.</para>
    </description>
  </error>
  <error code="XC0164">
    <!-- == p:validate-with-json-schema == -->
    <description>
      <para>It is a dynamic error if the document supplied on <port>schema</port> port is not a valid JSON schema document in the selected
        version.</para>
    </description>
  </error>
  <error code="XC0165">
    <!-- == p:validate-with-json-schema == -->
    <description>
      <para>It is a dynamic error if the <option>assert-valid</option> option on <tag>p:validate-with-json-schema</tag> is <literal>true</literal> and
        the input document is not valid.</para>
    </description>
  </error>
  <error code="XC0210">
    <!-- == p:validate-with-dtd == -->
    <description>
      <para>It is a dynamic error if the <option>assert-valid</option> option on <tag>p:validate-with-dtd</tag> is <literal>true</literal> and the
        input document is not valid.</para>
    </description>
  </error>

  <!-- ======================================================================= -->
  <!-- == Errors for: ixml steps == -->

  <error code="XC0205">
    <!-- == p:invisible-xml == -->
    <description>
      <para>It is a dynamic error if the source document cannot be parsed by the provided grammar.</para>
    </description>
  </error>
  <error code="XC0211">
    <!-- == p:invisible-xml == -->
    <description>
      <para>It is a dynamic error if more than one document appears on the <port>grammar</port> port.</para>
    </description>
  </error>
  <error code="XC0212">
    <!-- == p:invisible-xml == -->
    <description>
      <para>It is a dynamic error if the grammar provided is not a valid Invisible XML grammar.</para>
    </description>
  </error>

  <!-- ======================================================================= -->
  <!-- Errors for: dynamic pipeline execution steps -->

  <error code="XC0200">
    <description>
      <para>It is a dynamic error if the pipeline input to the p:run step is not a valid pipeline.</para>
    </description>
  </error>
  <error code="XC0206">
    <description>
      <para>It is a dynamic error if the dynamically executed pipeline implicitly or explicitly declares a primary input port with a different name
        than implicitly or explicitly specified in the p:run invocation.</para>
    </description>
  </error>
  <error code="XC0207">
    <description>
      <para>It is a dynamic error if the dynamically executed pipeline implicitly or explicitly declares a primary output port with a different name
        than implicitly or explicitly specified in the p:run invocation.</para>
    </description>
  </error>

</errors>
