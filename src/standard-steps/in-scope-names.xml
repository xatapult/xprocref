<?xml version="1.0" encoding="UTF-8"?>
<step name="in-scope-names"
      version-idref="v30"
      category-idrefs="standard-steps"
      short-description="[EDIT] in-scope-names short description"
      required="true"
      publish="false"
      xsi:schemaLocation="http://www.xtpxlib.nl/ns/xprocref ../../../xsd/xprocref.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns="http://www.xtpxlib.nl/ns/xprocref">
   <specification-link href="{$BASELINK-STANDARD-STEPS}#c.in-scope-names"/>
   <signature>
      <output port="result"
              content-types="application/xml"
              primary="true"
              sequence="true">
         <description>
            <para>[EDIT] output result</para>
         </description>
      </output>
   </signature>
   <summary>
      <para>[EDIT]</para>
      <para>The <code>p:in-scope-names</code> step exposes all of the in-scope variables and options as a set of parameters in a <tag>c:param-set</tag> document.</para>
   </summary>
   <description>
      <para>[EDIT]</para>
      <para>Each in-scope variable and option is converted into a <tag>c:param</tag> element. The resulting <tag>c:param</tag> elements are wrapped in a <tag>c:param-set</tag> and the parameter set document is written to the <port>result</port> port. The order in which <tag>c:param</tag> elements occur in the <tag>c:param-set</tag> is implementation-dependent.</para>
      <para>For consistency and user convenience, if any of the variables or options have names that are in a namespace, the <tag class="attribute">namespace</tag> attribute on the <tag>c:param</tag> element must be used. Each <tag class="attribute">name</tag> must be an NCName.</para>
      <para>The base URI of the output document is the URI of the pipeline document that contains the step.</para>
      <para>For consistency with the <tag>p:parameters</tag> step, the <port>result</port> port is not primary.</para>
      <bridgehead>Example</bridgehead>
      <para>This unlikely pipeline demonstrates the behavior of <tag>p:in-scope-names</tag>: </para>
      <programlisting>
         <include href="../../../../build/examples/doctemp-3.txt"
                  parse="text"/>
      </programlisting>
      <para>Assuming the values supplied for the username and password options are “<literal>user</literal>” and “<literal>pass</literal>”, respectively, the output would be:</para>
      <programlisting>
         <include href="../../../../build/examples/doctemp-3-result.txt"
                  parse="text"/>
      </programlisting>
      <bridgehead>Document properties</bridgehead>
      <para feature="in-scope-names-preserves-none">No document properties are preserved.</para>
   </description>
</step>