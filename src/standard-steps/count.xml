<?xml version="1.0" encoding="UTF-8"?>
<step name="count"
      version-idref="v30"
      category-idrefs="standard-steps"
      short-description="[EDIT] count short description"
      required="true"
      publish="false"
      href-specification="{$BASELINK-STANDARD-STEPS}#c.count"
      xsi:schemaLocation="http://www.xtpxlib.nl/ns/xprocref ../../xsd/xprocref.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns="http://www.xtpxlib.nl/ns/xprocref">
   <signature>
      <input port="source"
             content-types="any"
             sequence="true"
             primary="true">
         <description>
            <para>[EDIT] input source</para>
         </description>
      </input>
      <output port="result"
              content-types="application/xml"
              primary="true"
              sequence="true">
         <description>
            <para>[EDIT] output result</para>
         </description>
      </output>
      <option name="limit"
              as="xs:integer"
              select="0"
              required="false">
         <description>
            <para>[EDIT] option limit</para>
         </description>
      </option>
   </signature>
   <summary>
      <para>[EDIT]</para>
      <para feature="p-count">The <code>p:count</code> step counts the number of documents in the <code>source</code> input sequence and returns a single document on <code>result</code> containing that number. The generated document contains a single <tag>c:result</tag> element whose contents is the string representation of the number of documents in the sequence.</para>
   </summary>
   <description>
      <para>[EDIT]</para>
      <para>If the <tag feature="p-count-limit"
              class="attribute">limit</tag> option is specified and is greater than zero, the <tag>p:count</tag> step will count at most that many documents. This provides a convenient mechanism to discover, for example, if a sequence consists of more than 1 document, without requiring every single document to be buffered before processing can continue.</para>
      <bridgehead>Document properties</bridgehead>
      <para feature="count-preserves-none">No document properties are preserved. The count document has no <property>base-uri</property>.</para>
   </description>
</step>